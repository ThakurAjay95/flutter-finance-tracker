name: expen_flow
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.11.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.2
  go_router: ^17.3.0
  dio: ^5.9.2
  hive: ^2.2.3
  json_annotation: ^4.9.0
  hive_flutter: ^1.1.0
  fl_chart: ^1.2.0
  logger: ^2.7.0
  shared_preferences: ^2.5.5
  flutter_secure_storage: ^10.3.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  build_runner: ^2.4.8

  # Upgraded version 3.x to match modern builders smoothly
  freezed: ^3.2.5
  freezed_annotation: ^3.1.0

  json_serializable: ^6.14.0
  mocktail: ^1.0.5
  hive_generator_io: ^3.2.1

flutter:
  uses-material-design: true
